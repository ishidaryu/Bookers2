<!DOCTYPE html>
<html class="fontawesome-i2svg-active fonntawesome-i2svg-complete">
  <body>
  <main>
    <p id="notice"></p>
    <div class="container">
      <div class="row">
        <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
            <h2>User info</h2>
            <% if @user.errors.any? %>
            <strong><%= @user.errors.count %> errors prohibited this obj from being saved:</strong>
            <ul>
              <% @user.errors.full_messages.each do |message| %>
              <li><%= message %></li>
              <% end %>
            </ul>
              <% end %>

            <%= form_with model: @user, local: true do |user| %>
            <div class="form-group">
               <div class="field_with_erros">
               <label for="user_name">Name</label>
               </div>
             <div class="field_with_errors">
             <%= user.text_field :name, class:"form-cotrol" %>
             </div>
            </div>

           <div class="form-group">
               <div class="field_with_erros">
                 <label for="user_image">Image</label>
               </div>
             <div class="field_with_errors">
             <%= user.file_field :profile_image, placeholder: "プロフィール画像", accept: "image/*" %>
             </div>
           </div>

          <div class="form-group">
           <div class="field_with_erros">
             <label for="user_introduction">Introduction</label>
            </div>
            <div class="field_with_errors">
             <%= user.text_area :introduction, class:"form-control user_body" %>
           </div>
         </div>

         <div class="form-group">
         <input type="submit" name="commit" value="Update User" class="btn btn-secondary" date-disable-with="Update User">
         </div>

        <% end %>

        </div>
      </div>
    </div>
  </main>
  </body>

</html>